[project]
name = "finance-payment-recon"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pyspark>=3.5", "common-spark"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/finance_payment_recon"]
