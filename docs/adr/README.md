# Architecture Decision Records

One markdown file per significant decision. Use the format:

```
00NN-<slug>.md
```

Required for: cross-team data flows, new patterns, Restricted-data
touches, and any non-obvious architectural choice.

See `0001-monorepo-architecture.md` for the format.
