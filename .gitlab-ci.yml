# CDO platform — GitLab CI/CD.
# Minimal pipeline: lint only until Artifactory network access is resolved.

stages:
  - lint

default:
  image: ghcr.io/astral-sh/uv:python3.11-bookworm

lint:
  stage: lint
  script:
    - uvx ruff check .
    - uvx ruff format --check .
