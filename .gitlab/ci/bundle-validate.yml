bundle-validate:
  stage: bundle-validate
  image: databricks/cli:latest
  script:
    - databricks bundle validate
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
    - if: $CI_COMMIT_BRANCH =~ /^release\//
    - when: manual
      allow_failure: true
