# .gitlab/

GitLab-specific configuration. CI pipeline fragments and MR templates.

| Folder / file | What |
|---|---|
| `ci/` | Per-stage CI YAML files included by `.gitlab-ci.yml`. |
| `merge_request_templates/default.md` | The template every MR opens with. |
